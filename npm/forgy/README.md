# Forgy

Forgy installs the Rust `forge` CLI from npm.

```sh
npm install -g forgy
forge doctor
```

The actual app is written in Rust. This package only downloads the matching prebuilt binary from GitHub Releases.

Repository: <https://github.com/jasperdevs/Forgy>
